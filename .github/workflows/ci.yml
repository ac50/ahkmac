name: CI

on:
  push:
    branches: [main, advanced, 'feat/**']
    tags: ['v*']
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build (release)
        run: swift build -c release
      - name: Run unit tests
        run: swift test --parallel
      - name: Smoke-check example config
        run: .build/release/ahkmac --check examples/ahkmac.conf
      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ahkmac-${{ github.sha }}
          path: .build/release/ahkmac

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: test
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Build arm64 binary
        run: |
          swift build -c release
          lipo -archs .build/release/ahkmac | grep -qw arm64
          cp .build/release/ahkmac ahkmac-arm64
      - name: Assemble app bundle
        run: |
          mkdir -p ahkmac.app/Contents/MacOS
          cp ahkmac-arm64 ahkmac.app/Contents/MacOS/ahkmac
          sed "s/__VERSION__/${GITHUB_REF_NAME#v}/g" packaging/Info.plist > ahkmac.app/Contents/Info.plist
          codesign --force --sign - ahkmac.app
          ditto -c -k --keepParent ahkmac.app ahkmac.app.zip
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" ahkmac-arm64 ahkmac.app.zip --title "$GITHUB_REF_NAME" --generate-notes
